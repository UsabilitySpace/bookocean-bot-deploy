
# updates usability space bot engine

# (still under development)

SCRIPT_DIR="$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )"
source $SCRIPT_DIR/@bash_inc_deploy__BASE

curdir="$PWD"
cd "/var/www/www-root/data/nodejs/server"

# this will throw error if bot engine has not been init at leas once:

cd usability-space-bot-engine

# this will throw error if bot engine has not been init at leas once:

md bot
md config
md deploy
md menu

ll

# this will throw error if bot engine has not been init at leas once:

git clone --single-branch --branch master https://UsabilitySpace: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@github.com/UsabilitySpace/bookocean-bot.git        bot
git clone --single-branch --branch master https://UsabilitySpace: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@github.com/UsabilitySpace/bookocean-bot-config.git config
git clone --single-branch --branch master https://UsabilitySpace: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@github.com/UsabilitySpace/bookocean-bot-deploy.git deploy
git clone --single-branch --branch master https://UsabilitySpace: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@github.com/UsabilitySpace/bookocean-bot-menu.git   menu

echo "Updating of usability space bot engine has finished."
echo "See for absense of errors (above) to check if the updating has been successful."

cd curdir