
# updates usability space bot engine

# (still under development)

SCRIPT_DIR="$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )"
source $SCRIPT_DIR/@bash_inc_deploy__BASE

curdir="$PWD"
cd "/var/www/www-root/data/nodejs/server"

# this will throw error if bot engine has been init at leas once:

md usability-space-bot-engine
cd usability-space-bot-engine

# this will throw error if bot engine has been init at leas once:

md bot
md config
md deploy
md menu

ll

# this will throw error if bot engine has been init at leas once:

git clone --single-branch --branch master https://UsabilitySpace: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@github.com/UsabilitySpace/bookocean-bot.git bot

echo "Creation of a new usability space bot engine has finished."
echo "See for absense of errors (above) to check if the creation process has been successful."

cd curdir