
# updates usability space bot engine

# (still under development)

SCRIPT_DIR="$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )"
source $SCRIPT_DIR/engine_redeploy_scripts

curdir="$PWD"
cd "/var/www/www-root/data/nodejs/server"

# this will throw error if bot engine has not been init at leas once:

cd usability-space-bot-engine

# this will throw error if bot engine has not been init at leas once:

ll

# this will throw error if bot engine has not been init at leas once:

cd bot ; git pull
cd config ; git pull
cd deploy ; git pull
cd menu ;  ; git pull

echo "Updating of the existing bot engine has finished."
echo "See for absense of errors above to check if the updating has been successful."

cd curdir

# ─────────────────────────────────

# value added in all public files

SCRIPT_DIR="$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )"
source $SCRIPT_DIR/engine_redeploy_scripts

# ─────────────────────────────────

# must do nothing else ■
